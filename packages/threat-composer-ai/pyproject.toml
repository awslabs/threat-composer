# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

[project]
name = "threat-composer-ai"
version = "0.1.1"
description = "AI-powered automated threat modeling for codebases"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "bandit",
  "boto3",
  "botocore[crt]",
  "click",
  "diagrams",
  "fastmcp",
  "jschema-to-python",
  "json-schema-to-pydantic",
  "jsonpickle",
  "opentelemetry-api",
  "opentelemetry-exporter-otlp-proto-http",
  "opentelemetry-instrumentation-fastapi",
  "opentelemetry-propagator-aws-xray",
  "opentelemetry-sdk",
  "pathspec",
  "psutil",
  "rich",
  "sarif-om",
  "strands-agents",
  "strands-agents-tools"
]

  [project.scripts]
  threat-composer-ai-cli = "threat_composer_ai.__main__:main"
  threat-composer-ai-mcp = "threat_composer_ai.mcp_server:main"
  threat-composer-ai-eval-quality = "threat_composer_ai.eval.quality:main"

  # Kept out of the runtime dependencies so that installing the CLI or the MCP
  # server does not pull in an evaluation framework only CI uses. Install with
  # `uv sync --extra eval`. All of its own core dependencies are already resolved
  # here transitively, so this adds a single package.
  [project.optional-dependencies]
  eval = [
    "strands-agents-evals"
  ]

[dependency-groups]
dev = [
  "pytest>=9.0.3",
  "pytest-mock",
  "pytest-cov",
  "pytest-asyncio",
  "ruff"
]

[tool.uv]
managed = true
package = true

[tool.ruff]
line-length = 88
target-version = "py310"
exclude = [
  ".git",
  "__pycache__",
  ".venv",
  ".eggs",
  "*.egg",
  "dist",
  "build"
]

  [tool.ruff.lint]
  select = [ "E", "W", "F", "I", "B", "C4", "UP" ]
  ignore = [ "E501" ]

  [tool.ruff.format]
  quote-style = "double"
  indent-style = "space"
  skip-magic-trailing-comma = false
  line-ending = "auto"
