{
  "_comment": [
    "Quality eval expectations for the threat-composer-app-browser-extension fixture.",
    "",
    "Kept as data rather than code so that retuning a band is a reviewable one line",
    "change instead of a patch to Python.",
    "",
    "Two reference runs, both on strands-agents 1.53.0 and",
    "global.anthropic.claude-sonnet-5, against the same unchanged fixture:",
    "",
    "            threats  mitigations  assumptions  total tokens  wall clock",
    "  run 1          19           31           20     1,255,735       1066s",
    "  run 2          20           25           20       823,319        550s",
    "",
    "Bands are wide on purpose, and those two runs are why. Same input, same model,",
    "same code: mitigations moved by 19% and token spend by 34%. Pinning anywhere",
    "near run 1's figures would have failed run 2 for no reason at all. A band's job",
    "is to catch collapse or runaway, not to pin a figure, so each is set roughly",
    "half to double."
  ],

  "case_name": "browser-extension-threat-model",

  "_fixture": [
    "The eval's input, held still so that the agent is the only thing that moves.",
    "Without this the eval cannot do its job: if the fixture drifts, a score change",
    "means either the agent regressed or the input changed, and the result cannot",
    "tell you which.",
    "",
    "Two mechanisms, because they do different jobs and neither substitutes for the",
    "other.",
    "",
    "  commit    supplies the source. The workflow checks this commit out and",
    "            analyses that, so the eval keeps producing comparable numbers no",
    "            matter what later happens to the fixture on main. Without it, the",
    "            first person to edit the browser extension would stop the eval",
    "            running at all, which is close to the opposite of what it is for.",
    "",
    "  tree_sha256  verifies the source. Cheap defence against getting the wrong",
    "            tree anyway, whether through a checkout misconfiguration, a local",
    "            edit, or a pin left stale. Checked before the CLI runs, so a",
    "            mismatch costs nothing rather than eighteen minutes of inference.",
    "",
    "The hash covers relative paths and file bytes for all 49 tracked files,",
    "excluding build output and dependency trees. tests/eval/test_fixture.py asserts",
    "the hashed set matches what git tracks, so those exclusions cannot silently",
    "diverge from the repository.",
    "",
    "commit is null until this stack lands. The bands below were measured against the",
    "fixture as it exists on this branch, which differs from main because the Vite",
    "migration rewrites it, and this repository squash merges and deletes branches so",
    "a branch commit would be orphaned. Once the stack merges, set commit to the",
    "resulting squash commit on main: the fixture there will be exactly what was",
    "measured, so tree_sha256 below will already match. While commit is null the",
    "workflow analyses the checked-out tree, still hash verified.",
    "",
    "To reproduce the pinned fixture locally:",
    "  git worktree add /tmp/fixture <commit>",
    "  python -m threat_composer_ai.eval.quality --target /tmp/fixture/<path> ...",
    "",
    "Changing the fixture is allowed, but deliberate. Re-run, sanity check the new",
    "figures, then update commit, tree_sha256 and the bands together in one",
    "reviewable change."
  ],
  "fixture": {
    "path": "packages/threat-composer-app-browser-extension",
    "commit": null,
    "tree_sha256": "02f39718f292ebaefb644b9b6b345dbff853da520577e3acad163347402101ec",
    "file_count": 49,
    "byte_count": 185987
  },

  "bands": {
    "threats": [10, 40],
    "mitigations": [15, 65],
    "assumptions": [8, 45]
  },

  "min_statement_chars": 40,
  "min_diagram_text_nodes": 10,

  "_allow_incomplete_threats": [
    "Number of threats permitted to leave grammar fields empty. Zero to begin with,",
    "on the basis that the schema models the grammar and the agent should fill it.",
    "Raise it if the reference run shows this is unrealistically strict rather than",
    "quietly dropping the check."
  ],
  "allow_incomplete_threats": 0,

  "_max_total_tokens": [
    "Cost regression guard, at roughly double the reference run. Nothing in the",
    "artifacts notices a prompt change that doubles spend, and at over a million",
    "tokens a run that is worth catching."
  ],
  "max_total_tokens": 2500000,

  "_telemetry": [
    "require_spans asserts telemetry actually produced spans. Without it, every",
    "tool assertion below passes vacuously the moment telemetry breaks, which is",
    "worse than not having them. expected_tools are the tools the workflow cannot",
    "complete without."
  ],
  "require_spans": true,
  "min_spans": 20,
  "expected_tools": [
    "threat_composer_workdir_file_write"
  ],

  "_concept_groups": [
    "Ideas any competent threat model of this extension ought to raise. Each group",
    "holds alternative phrasings and matches if ANY one appears, case-insensitively.",
    "That is deliberate: a model says 'background service worker' one run and",
    "'background script' the next, so exact strings would fail for reasons that have",
    "nothing to do with quality.",
    "",
    "Occurrence counts in the reference run are given below, and are why these groups",
    "were chosen over weaker signals such as CSP or DOMPurify, which appeared once",
    "each and would make the check brittle.",
    "",
    "min_concept_groups is 6 of 8, leaving room for phrasing to drift while still",
    "failing if the output collapses into text that would fit any codebase."
  ],
  "min_concept_groups": 6,
  "concept_groups": {
    "background execution context": [
      "service worker",
      "background worker",
      "background script",
      "background page"
    ],
    "content script injection": [
      "content script",
      "content-script",
      "injected script"
    ],
    "extension message passing": [
      "runtime.onMessage",
      "onMessage",
      "sendMessage",
      "postMessage",
      "message passing",
      "runtime message"
    ],
    "execution world isolation": [
      "isolated world",
      "main world",
      "page context"
    ],
    "manifest and host permissions": [
      "manifest",
      "host permission",
      "match pattern",
      "permissions"
    ],
    "manifest version differences": [
      "MV3",
      "MV2",
      "manifest v3",
      "manifest v2",
      "manifest_version"
    ],
    "untrusted threat model input": [
      ".tc.json",
      "threat model json",
      "threat composer json",
      "threat model file"
    ],
    "extension user interface surface": [
      "popup",
      "options page",
      "action popup",
      "new tab"
    ]
  }
}
